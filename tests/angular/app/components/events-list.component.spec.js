ngDescribe({
    name: 'Test events-list component',
    modules: 'app',
    element: '<events-list></events-list>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
